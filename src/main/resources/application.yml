server:
  port: 8081
  servlet:
    session:
      cookie:
        max-age: 604800
spring:
  mvc:
    hiddenmethod:
      filter:
        enabled: true
  application:
    name: gen-net-app
  data:
    rest:
      base-path: /api